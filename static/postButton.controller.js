//angular scripting for user feed tab


//needs to be unified with other controller nomenclature
angular.module('ngToggle', [])
    .controller('AppCtrl',['$scope', function($scope){
        $scope.custom = true;
}]);
